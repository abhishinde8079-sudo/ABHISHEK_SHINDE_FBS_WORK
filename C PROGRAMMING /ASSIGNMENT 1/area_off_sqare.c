#include<stdio.h>
int main()
{
    float side,area;
    printf("Enter your side to calculate area off sqare: ");
    scanf("%f",&side);
    
    area=side*side;
    printf("area off sqare is=%f",area);
	return 0;
}